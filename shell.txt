Compter le nombre ligne  csv: mawk -F ';' '{print $6}' data.csv | sort | uniq -c | mawk '{sum += $1} END {print sum}'



Vérification que la commande est bonne de traitement d1 est bonne:

mawk -F ';' '{if (!seen[$6,$1]++) count[$6]++} END {for (conducteur in count) print conducteur, count[conducteur]}' data.csv | sort -k3,3nr | mawk '{total += $3} END {print "Total:", total}'




Traitement d1 data.csv:

mawk -F ';' '{if (!seen[$6,$1]++) count[$6]++} END {for (conducteur in count) print conducteur, count[conducteur]}' data.csv | LC_ALL=C sort -k3,3nr -S 50% | head -n 10 > resultat_chauffeur.txt




Vérifier le temps d'exécution de d1 (environ 6-7 seconde):

time mawk -F ';' '{if (!seen[$6,$1]++) count[$6]++} END {for (conducteur in count) print conducteur, count[conducteur]}' data.csv | LC_ALL=C sort -k3,3nr -S 50% | head -n 10 > resultat_chauffeur.txt




Traitement d2 data.csv:

mawk -F ';' '{ conducteur[$6] += $5 } END { for (c in conducteur) print conducteur[c], c }' data.csv | sort -nr | head -n 10  




Vérifier le temps d'exécution de d2 (environ 2 seconde): 

time mawk -F ';' '{ conducteur[$6] += $5 } END { for (c in conducteur) print conducteur[c], c }' data.csv | sort -nr | head -n 10  




Traitement L data.csv:

mawk -F ';' '{ trajet[$1] += $5 } END { for (t in trajet) print t, trajet[t] }' data.csv | sort -k2,2nr | head -n 10 | sort -k1,1n > Trajet.txt




Vérifier le temps d'exécution de L (environ 3 seconde): 

time mawk -F ';' '{ trajet[$1] += $5 } END { for (t in trajet) print t, trajet[t] }' data.csv | sort -k2,2nr | head -n 10 | sort -k1,1n > Trajet.txt



Questions: 

Es ce que chaque ligne ou il y a un conducteur compte comme trajet ou une étape d'un trajet parce que pour le traitement d1 je ne sais pas si le chauffeur fait toutes les étapes des trajets, ainsi j'ai fait la somme du nombre de fois ou le chauffeur apparait dans une ligne du csv? (c'est des étapes de trajets)

Es ce que pour le traitement d2 on peut arrondir la somme des distances en entiers ou on doit les laisser en nombre décimaux pour la liste des 10 premiers conducteurs?
